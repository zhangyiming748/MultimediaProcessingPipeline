name: merge
services:
  cuda:
    container_name: cuda
    volumes:
      - ./:/data
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities:
                - gpu
    image: zhangyiming748/whisperWithCUDA
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,video,utility